# os specific
.DS_Store

# NOTE: {-lock.json} file required for {npm ci} command which is executed by Docker
# /package-lock.json
/npm-debug.log

# node specific
node_modules

# architecture base structure
dist

# secrets
.env*
!.env.example
